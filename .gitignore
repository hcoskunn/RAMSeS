# Virtual environments and IDE
.venv/
.idea/
.vscode/

# Python cache / build files
__pycache__/
*.py[cod]
*.egg-info/
*.pkl
*.log

# Outputs / results
myresults/
Mononito/
Mononito
output/
Outputs/
docs/
# Consolidated 2026-09-08: archive/ also holds the former
# results_{skab,skab-old,smd,ucr}/, testbed_results_{test,collected}/,
# Plots/, important-new-graphs/ and old/.
archive/
dataset_lists/
testbed_results/
Misc/
results/
shells/

# OS / Jupyter junk
.DS_Store
.ipynb_checkpoints/
RAMS-TSAD-Up-to-date.zip
# Series2Graph is fetched, never committed: it is patent-encumbered and licensed
# for research use only, unlike the Apache-2.0 TSB-AD code vendored around it.
#   python -m Algorithms.tsb_ad.fetch_series2graph
# See Algorithms/series2graph_detector.py for why.
Algorithms/tsb_ad/models/Series2Graph.py

# Model checkpoints and binaries (see also *.pkl above)
*.pth
*.pt
*.ckpt

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Editor state; .idea/ and .venv/ are listed at the top of this file.
venv/

# Scratch and one-off debug output
output1.txt
*.pptx

# Paper/demo working directory (large PDFs, drafts)
demo_paper_prep/

# Trained checkpoints for a single SMD entity; nothing in the codebase
# loads them (grep for saved_models returns only this line).
saved_models/
